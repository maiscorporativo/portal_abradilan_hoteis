# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local

# Environment variables — NEVER commit these
.env
.env.local
.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Uploads — precisam ser versionados: o deploy da Hostinger reconstrói a dist/
# a partir da public/, então uploads fora do git são apagados a cada deploy.
# Fluxo: subir a imagem pelo admin local, commitar public/uploads/ e fazer push.
